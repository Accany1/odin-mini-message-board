<html>
  <body>
    <h1><%- title %></h1>
    <% messages.map((message) => { %>
        <h2><%= message.user %></h2>
        <p><%= message.text %></p>
        <p><%= message.added %></p>
        <a href="/message/<%= message.id %>/details">Details</a>
      <% }) %>
  </body>
</html>